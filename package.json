{
  "name": "pnpr-cloudflare-containers",
  "description": "Example pnpr registry deployment on Cloudflare Containers with R2 storage",
  "dependencies": {
    "@cloudflare/containers": "0.0.30",
    "hono": "4.11.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "5.9.3",
    "wrangler": "^4.107.0"
  },
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  }
}
